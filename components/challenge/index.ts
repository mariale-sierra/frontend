// Challenge components organized by feature area.

// create/: Components for the multi-step challenge creation flow
// (header, title inputs, duration stepper, day assignment, visibility settings, submit actions).
export * from './create';

// detail/: Components for the challenge detail view
// (challenge header with metadata, routine list, and per-day routine cards).
export * from './detail';

// list/: Components for the challenges tab and discovery screen
// (section layout, active challenge cards, explore cards, progress bar, day labels, badges).
export * from './list';

// progress/: Components for the active challenge progress screen
// (static progress header, participant stack, pager dots, photo skeleton, and workout calendar).
export * from './progress';
